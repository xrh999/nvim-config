Make sure to use :Mason to install all the LSP servers needed, here's a command to install all the servers expected by lua/plugins.lua

:MasonInstall basedpyright bash-language-server clangd css-lsp html-lsp json-lsp lua-language-server marksman typescript-language-server tailwindcss-language-server

And then here's a command for the brew install list (MacOS):
brew install tree-sitter-cli yazi rg
